//Write a function that takes an unsigned integer and returns the number of '1' bits it has (also known as the Hamming weight).

//Solution

var hammingWeight = function(n) {
        
    return ones = n.toString(2).replace(/0/g,"").length;   
};
